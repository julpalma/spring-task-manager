version: '3.8'

services:
  app:
    build: .
    container_name: task-manager-app
    ports:
      - "8080:8080"  # App available at localhost:8080
    environment:
      SPRING_PROFILES_ACTIVE: default  # uses H2